upstream myproxy {
    server 51.250.110.4;
    server 84.201.152.30; # would need changes after server restarts
    server 84.201.152.101; # would need changes after server restarts

}

server {
    listen 80;
    server_name localhost;
    root /var/www/html;

    location / {
        proxy_pass http://myproxy;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}
